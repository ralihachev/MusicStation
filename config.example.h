#pragma once

// Copy this file to config.h and enter the 2.4 GHz Wi-Fi credentials used by
// MusicStation. config.h is ignored by Git and must never be committed.
#define WIFI_SSID "YOUR_WIFI_NAME"
#define WIFI_PASS "YOUR_WIFI_PASSWORD"
